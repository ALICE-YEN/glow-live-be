import { FastifyRequest, FastifyReply } from "fastify";
// import { getCategories } from "../services/streams.service";

export const createStreamHandler = async (
  request: FastifyRequest,
  reply: FastifyReply
) => {};
